//Call this script if you want to check you dont spawn on the donald
while (distance_to_object(obj_donald) < 100)
{
    //If we have a moving view and we want spawns only in our view
    //Change this to use view_xview[1]? -> 0 is left
    x = random(room_width);
    y = random(room_height);

}
